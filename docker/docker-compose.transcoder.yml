version: '3.8'

services:
  transcoder:
    build:
      context: ..
      dockerfile: docker/Dockerfile.transcoder
    container_name: iptv-transcoder
    restart: unless-stopped
    volumes:
      - ../input:/app/input
      - ../output:/app/output
    environment:
      - TZ=UTC
